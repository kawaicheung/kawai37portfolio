<%
  # Optional: reversed: true
%>
<% reversed ||= false %>
<div class="nav-container">
  <nav class="nav-map">
    <ul> 
      <% Constants::NAVIGATION_SECTIONS.each do |section| %>
        <li>
          <%= link_to section[:name], "/#{section[:path]}", class: "#{reversed ? "reversed" : ""} #{request.path.include?(section[:path]) ? "selected" : ""}" %>
        </li>
      <% end %>
    </ul>
  </nav>
  <!--<div class="nav-pages">
    <% Constants::NAVIGATION_PATHS.each do |path| %>
      <%= link_to "", send(path), class: "dot #{current_page?(send(path)) ? 'selected' : ''}" %>
    <% end %>
  </div>-->
</div>